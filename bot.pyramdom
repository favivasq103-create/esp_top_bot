@client.event
async def on_message(message):
    if message.author == client.user:
        return
    if message.content.startswith('$random') or message.content.startswith('$azar'):
        numero = random.randint(1, 100)
        await message.channel.send(f'Tu número aleatorio es: **{numero}**')
